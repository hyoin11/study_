package Java;

public class a05_Sysout {
	/*
	 * 자바 표준 입출력 클래스
	 * 자바에서는 입출력을 담당하는 수단 또한 모두 객체
	 * System이라는 표준 입출력 클래스를 정의하여 제공함
	 * 이러한 시스템 클래스는 java.lang 패키지에 포함되어 제공됨
	 * 시스템 클래스에는 표준 입출력을 위해 다음과 같은 클래스 변수(static variable)가 정의되어 있음
	 * 	1. System.in - 표준 입력 작업을 수행
	 * 	2. System.out - 표준 출력 작업을 수행
	 * 	3. System.err - 표준 출력 작업을 수행
	 * 
	 * System.out.println()
	 * 전달된 데이터를 출력한 후에 줄 바꿈까지 해줌
	 */
}
